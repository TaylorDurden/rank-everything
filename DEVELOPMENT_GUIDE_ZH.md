# Asset Rating Platform 开发指南

欢迎来到 Asset Rating Platform (多资产评级平台) 项目。本项目采用现代化的 Monorepo 架构，旨在提供高性能、可扩展的全栈开发体验。

## 🚀 技术栈

- **Monorepo 管理**: [Turborepo](https://turbo.build/)
- **包管理器**: [pnpm](https://pnpm.io/)
- **前端**: [Next.js 16 (App Router)](https://nextjs.org/) + [Tailwind CSS](https://tailwindcss.com/)
- **后端**: [NestJS 11](https://nestjs.com/)
- **共享配置**: ESLint, Prettier, TypeScript, Jest

---

## 📂 项目结构

```text
.
├── apps/
│   ├── web/                # Next.js 前端应用 (端口: 3001)
│   └── api/                # NestJS 后端应用 (端口: 3000)
├── packages/
│   ├── api/                # 共享后端类型及逻辑
│   ├── ui/                 # 共享 React 组件库
│   ├── eslint-config/      # 统一的代码校验配置
│   ├── jest-config/        # 统一的单元测试配置
│   └── typescript-config/  # 统一的 TypeScript 配置
├── prompt/
│   └── docs/               # 需求文档、架构设计、AI Prompt 等
├── turbo.json              # Turborepo 任务编排配置
└── pnpm-workspace.yaml     # pnpm 工作区配置
```

---

## 🛠️ 快速上手

### 1. 安装依赖
确保已安装 Node.js (推荐 v18+) 和 pnpm。
```bash
pnpm install
```

### 2. 本地开发
启动所有应用（前端和后端）的开发服务器：
```bash
pnpm dev
```
- 前端访问地址: `http://localhost:3001`
- 后端 API 地址: `http://localhost:3000`

### 3. 构建项目
构建所有应用和包：
```bash
pnpm build
```

---

## 💻 详细开发指南

### 前端开发 (apps/web)
- 使用 **Next.js App Router** 结构。
- 样式采用 **Tailwind CSS**。
- **添加组件**: 推荐使用 `shadcn/ui`。您可以运行：
  ```bash
  pnpm dlx shadcn@latest add [component-name]
  ```
- **调用后端**: 建议在 `apps/web/src/lib/api.ts` 中封装 API 请求逻辑。

### 后端开发 (apps/api)
- 标准的 **NestJS** 模块化代码风格。
- **DTO & 实体**: 建议将可共享的 API 接口定义放在 `packages/api` 中，以实现前后端类型安全。

### 共享包的使用
如果您在 `packages/` 下创建或修改了包，由于我们使用了 `workspace:*` 引用，Turborepo 会自动处理依赖链接。
- 在 `apps/web` 中使用 `packages/ui`:
  已经在 `apps/web/package.json` 的 `dependencies` 中引用了 `"@repo/ui": "workspace:*"`。

---

## ⚡ 常用指令

| 指令 | 描述 |
| :--- | :--- |
| `pnpm dev` | 运行所有应用的开发模式 |
| `pnpm build` | 构建所有包和应用 |
| `pnpm lint` | 执行全量代码校验 |
| `pnpm format` | 格式化所有代码 |
| `pnpm test` | 运行单元测试 |

### 任务过滤 (Filtering)
如果您只想操作特定的项目，可以使用 `--filter` 参数：
```bash
# 仅启动前端
pnpm dev --filter web

# 仅构建后端
pnpm build --filter api
```

---

## 📝 开发规范
- **代码提交**: 请在提交前运行 `pnpm lint` 和 `pnpm format`。
- **文档先行**: 在进行重大功能开发前，请先更新 `prompt/docs/` 下的相关设计文档。
